apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "psql.fullname" . }}-test-connection"
  labels:
    {{- include "psql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "psql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
